when:
  - event: cron
    cron: sync-incus
  - event: manual
  - event: push
    branch: main

steps:
  # ------------------------------------------------------------
  # 1. Сборка Go AST parser
  # ------------------------------------------------------------
  - name: build-parser
    image: golang:1.22
    commands:
      - cd src/go-ast-parser
      - go build -o parser main.go
      - echo "✅ Go AST parser built"

  # ------------------------------------------------------------
  # 2. Мониторинг изменений (без клонирования Incus)
  # ------------------------------------------------------------
  - name: monitor-changes
    image: python:3.12-slim
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      INCUS_SOURCE_REPO:
        from_secret: incus_source_repo
    volumes:
      - /home/dev/woodpecker-state:/ci-state
    commands:
      - pip install --quiet PyGithub==2.5.0
      - mkdir -p state
      - mkdir -p /ci-state/incus-sync
      - |
        python -c "
        import json, os
        from pathlib import Path
        from src.monitor import run_monitor
        res = run_monitor(
            repo_name=os.environ['INCUS_SOURCE_REPO'],
            state_file=Path('/ci-state/incus-sync/.sync_state'),
            output_file=Path('state/changes.json'),
        )
        print(json.dumps(res, indent=2, ensure_ascii=False))

        Path('state').mkdir(exist_ok=True)
        if res.get('no_changes'):
            Path('state/.no_changes').write_text('true')
            print('NO_CHANGES detected')
        else:
            Path('state/.no_changes').write_text('false')
            Path('state/.incus_sha').write_text(res.get('last_sha', ''))
            api_count = len(res.get('api_changes', {}).get('files', []))
            client_count = len(res.get('client_changes', {}).get('files', []))
            Path('state/.api_changed_count').write_text(str(api_count))
            Path('state/.client_changed_count').write_text(str(client_count))
            print(f'Changes detected: API={api_count}, client={client_count}')
        "
      - echo "✅ Monitoring finished"
    depends_on:
      - build-parser

  # ------------------------------------------------------------
  # 3. Клонирование/обновление Incus source repo (shallow + cache)
  # ------------------------------------------------------------
  - name: clone-incus-source
    image: alpine/git
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      INCUS_SOURCE_REPO:
        from_secret: incus_source_repo
    volumes:
      - /home/dev/woodpecker-state:/ci-state
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping Incus clone"
          exit 0
        fi
      - git config --global --add safe.directory '*'
      - mkdir -p /ci-state/incus-src
      - cd /ci-state/incus-src
      - |
        if [ -d ".git" ]; then
          echo "📦 Incus repo cache found, updating..."
          git fetch --depth=1 origin HEAD
          git reset --hard FETCH_HEAD
          git clean -fd
        else
          echo "📦 First clone of Incus repo (shallow)..."
          git clone --depth=1 "https://x-access-token:$${GITHUB_TOKEN}@github.com/$${INCUS_SOURCE_REPO}.git" .
        fi
      - echo "✅ Incus source repo ready"
    depends_on:
      - monitor-changes

  # ------------------------------------------------------------
  # 4. Генерация DTO
  # ------------------------------------------------------------
  - name: generate-models
    image: rust:1.83
    environment:
      INCUS_REPO_PATH: /ci-state/incus-src
      OUTPUT_DIR: /woodpecker/src/github.com/NightSolist/dto_converter/generated_output
      OLLAMA_URL:
        from_secret: ollama_url
    volumes:
      - /home/dev/woodpecker-state:/ci-state
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping generation"
          exit 0
        fi
      - apt-get update -qq
      - apt-get install -y -qq python3 python3-pip
      - pip3 install --quiet --break-system-packages requests==2.32.3 Jinja2==3.1.4
      - rm -rf generated_output
      - mkdir -p generated_output
      - |
        python3 -c "
        import sys
        from src.pipeline import Pipeline
        sys.exit(0 if Pipeline().run() else 1)
        "
      - echo "✅ DTO generation completed"
      - |
        count=$$(find generated_output -maxdepth 1 -name "*.rs" | wc -l)
        echo "Generated files: $${count}"
        echo "$${count}" > state/.generated_count
    depends_on:
      - clone-incus-source

  # ------------------------------------------------------------
  # 5. Клонирование целевого Rust-репозитория
  # ------------------------------------------------------------
  - name: clone-rust-target
    image: alpine/git
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      RUST_TARGET_REPO:
        from_secret: rust_target_repo
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping Rust target clone"
          exit 0
        fi
      - git config --global --add safe.directory '*'
      - rm -rf target-rust
      - 'git clone --depth=1 "https://x-access-token:$${GITHUB_TOKEN}@github.com/$${RUST_TARGET_REPO}.git" target-rust'
      - echo "✅ Rust target repo cloned"
    depends_on:
      - generate-models

  # ------------------------------------------------------------
  # 6. Подмена generated_prototype в целевом Rust-репозитории
  # ------------------------------------------------------------
  - name: inject-generated
    image: alpine:latest
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping inject"
          exit 0
        fi
      - rm -rf target-rust/src/incus/generated_prototype/*.rs
      - cp generated_output/*.rs target-rust/src/incus/generated_prototype/
      - echo "✅ Generated files copied into Rust target repo"
    depends_on:
      - clone-rust-target

  # ------------------------------------------------------------
  # 7. Форматирование Rust-кода
  # ------------------------------------------------------------
  - name: rustfmt
    image: rust:1.83
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping rustfmt"
          exit 0
        fi
      - rustup component add rustfmt
      - cd target-rust
      - cargo fmt --all
      - echo "✅ Rust code formatted"
      - cp src/incus/generated_prototype/*.rs ../generated_output/
    depends_on:
      - inject-generated

  # ------------------------------------------------------------
  # 8. Компиляционная проверка Rust-репозитория
  # ------------------------------------------------------------
  - name: cargo-check
    image: rust:1.83
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping cargo check"
          exit 0
        fi
      - cd target-rust
      - cargo check --all-targets
      - echo "✅ cargo check passed"
    depends_on:
      - rustfmt

  # ------------------------------------------------------------
  # 9. Unit-тесты DTO
  # ------------------------------------------------------------
  - name: cargo-test-serde
    image: rust:1.83
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping serde tests"
          exit 0
        fi
      - cd target-rust
      - cargo test --test serde_dto -- --nocapture
      - echo "✅ serde DTO tests passed"
    depends_on:
      - cargo-check

  # ------------------------------------------------------------
  # 10. Публикация Pull Request
  # ------------------------------------------------------------
  - name: publish-pr
    image: python:3.12-slim
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
      RUST_TARGET_REPO:
        from_secret: rust_target_repo
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping PR publication"
          exit 0
        fi
      - pip install --quiet PyGithub==2.5.0
      - |
        python -c "
        import os
        from pathlib import Path
        from src.github_publisher import GitHubPublisher
        pub = GitHubPublisher(repo_name=os.environ['RUST_TARGET_REPO'])
        url = pub.publish(
            generated_dir=Path('generated_output'),
            target_path='src/incus/generated_prototype',
        )
        Path('state/.pr_url').write_text(url)
        print(f'PR created: {url}')
        "
      - echo "✅ Pull Request published"
    depends_on:
      - cargo-test-serde

  # ------------------------------------------------------------
  # 11. Обновление persistent state
  # ------------------------------------------------------------
  - name: update-state
    image: python:3.12-slim
    volumes:
      - /home/dev/woodpecker-state:/ci-state
    commands:
      - |
        if [ "$(cat state/.no_changes 2>/dev/null)" = "true" ]; then
          echo "⏭️  No changes detected, skipping state update"
          exit 0
        fi
      - |
        python -c "
        import json
        from pathlib import Path
        from src.monitor import save_state
        sha = json.loads(Path('state/changes.json').read_text(encoding='utf-8')).get('last_sha')
        if sha:
            save_state(sha, Path('/ci-state/incus-sync/.sync_state'))
            print(f'State updated: {sha}')
        "
      - echo "✅ Sync state updated"
    depends_on:
      - publish-pr

  # ------------------------------------------------------------
  # 12. Уведомление: успех
  # ------------------------------------------------------------
  - name: notify-success
    image: deblan/woodpecker-email
    settings:
      dsn:
        from_secret: email_dsn
      from:
        address:
          from_secret: email_username
        name: Incus Sync Bot
      recipients:
        from_secret: email_recipient
      recipients_only: true
      content_type: text/plain
      subject: "✅ Incus Sync: синхронизация выполнена (pipeline #${CI_PIPELINE_NUMBER})"
      body: |
        Здравствуйте!

        Pipeline Incus Model Sync успешно завершён.

        Сводка:
          • Репозиторий генератора: ${CI_REPO}
          • Pipeline: ${CI_PIPELINE_URL}
          • Подробности: смотрите файлы в state/ workspace pipeline.

        Что было выполнено:
          • Мониторинг изменений Incus
          • Парсинг Go-структур через AST
          • Шаблонная генерация DTO (template-first)
          • LLM repair (если потребовалось)
          • Форматирование cargo fmt
          • Компиляционная проверка cargo check
          • Unit-тесты DTO (serde_dto)
          • Создание Pull Request
          • Обновление .sync_state

        —
        Incus Sync Bot
    when:
      - status: success
    depends_on:
      - update-state

  # ------------------------------------------------------------
  # 13. Уведомление: ошибка
  # ------------------------------------------------------------
  - name: notify-failure
    image: deblan/woodpecker-email
    settings:
      dsn:
        from_secret: email_dsn
      from:
        address:
          from_secret: email_username
        name: Incus Sync Bot
      recipients:
        from_secret: email_recipient
      recipients_only: true
      content_type: text/plain
      subject: "⚠️ Incus Sync: ошибка синхронизации (pipeline #${CI_PIPELINE_NUMBER})"
      body: |
        Здравствуйте!

        Pipeline Incus Model Sync завершился с ошибкой.

        Сводка:
          • Репозиторий генератора: ${CI_REPO}
          • Pipeline: ${CI_PIPELINE_URL}

        Возможные этапы отказа:
          • Мониторинг изменений
          • Клонирование Incus
          • Генерация DTO
          • LLM repair
          • Rustfmt / cargo check / serde_dto
          • Публикация PR

        Требуется анализ логов pipeline.

        —
        Incus Sync Bot
    when:
      - status: failure